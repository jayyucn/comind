# T4/T5 实现记录 — 2026-07-15

## T4 · DateRefExtension — 完成 ✅

### 遗留问题
`handleClick` 的测试策略改为直接测试业务逻辑（不再试图调用 `handleClick` 本身）：
- `this.key.getState` 依赖 ProseMirror 插件上下文，jsdom 下无法可靠模拟
- 最终方案：提取 `handleClick` 的事件派发逻辑，直接调用，验证 payload 和事件名

### 测试结果
9 项全部通过（7 decoration 测试 + 2 handleClick 业务逻辑测试）

---

## T5 · useContentRenderer — 完成 ✅

### 实现要点
1. **引入 T1 API**：`DATE_REF_REGEX`、`parseDateRefs`、`formatDateRefDisplay`、`normalizeRecurrence`
2. **`renderDateRefs(text)`**：遍历 `DATE_REF_REGEX` 匹配项，渲染为 `<span class="date-ref" data-kind data-iso data-recurrence>显示文本</span>`
3. **渲染流水线顺序**（防止 `[[...]]` 冲突）：
   - `renderTypedLinks` → `renderDateRefs` → 外部链接 → WikiLink
4. **显示文本**：复用 T1 的 `formatDateRefDisplay`，与 editor 装饰层完全一致
5. **HTML 安全**：所有属性值经 `escapeHtmlEntities` 转义

### 测试结果
44 项全部通过（含 9 项新增 dateRef 测试）

### 测试覆盖
- `{{schedule:2026-07-15}}` 基础渲染
- `{{deadline:2026-07-15T14:00|weekly}}` 带时间+重复
- 无 recurrence → `data-recurrence="none"`
- daily/monthly/yearly 各规则
- 显示文本含格式化日期
- 多个 dateRef 各自渲染
- 与 wiki link 混合
- 与 #tag 混合
- HTML 注入安全
