# T11 实现记录 — 2026-07-15

## T11 · 自动推进 dateRef（Done 语义） — 完成 ✅

### 实现位置

`src/stores/property.ts`

### 核心逻辑

```ts
async function setProperty(blockId, key, value, type) {
  // ... 设置属性 ...

  // T11: 自动推进 dateRef（Done 语义）
  if (key === 'status' && value === 'Done') {
    await advanceDateRefInBlock(blockId)
  }

  return prop
}

async function advanceDateRefInBlock(blockId) {
  const block = blockStore.blocks.find(b => b.id === blockId)
  if (!block || !block.content) return

  const refs = parseDateRefs(block.content)
  const refsToAdvance = refs.filter(ref => ref.recurrence && ref.recurrence !== 'none')
  if (refsToAdvance.length === 0) return

  // 推进日期
  let newContent = block.content
  for (const ref of refsToAdvance) {
    const nextIso = calculateNextRecurrence(ref.iso, ref.recurrence)
    const oldText = serializeDateRef(ref)
    const newText = serializeDateRef({ kind: ref.kind, iso: nextIso, recurrence: ref.recurrence })
    newContent = newContent.replace(oldText, newText)
  }

  // 更新 content
  await blockStore.updateBlockContent(blockId, newContent)

  // 重置 status 为 Todo
  await setProperty(blockId, 'status', 'Todo', 'string')
}
```

### 行为

| 场景 | 结果 |
|------|------|
| `status=Done` + content 含 `weekly` dateRef | 日期 +7 天，status 重置为 Todo |
| `status=Done` + content 含 `daily` dateRef | 日期 +1 天，status 重置为 Todo |
| `status=Done` + content 含 `monthly` dateRef | 日期 +1 月（处理月末），status 重置为 Todo |
| `status=Done` + content 无 recurrence | 不推进，status 保持 Done |
| `status=Done` + content 无 dateRef | 不推进，status 保持 Done |

### 测试结果

- T11 测试 6/6 通过
- 覆盖：weekly/daily/monthly 推进、无 recurrence 跳过、无 dateRef 跳过、block 不存在跳过

### 注意事项

- `advanceDateRefInBlock` 递归调用 `setProperty` 设置 `status=Todo`，但因为此时 content 已无带 recurrence 的 dateRef，不会无限循环
- `property.test.ts` 有 9 项旧有测试失败（mock 不完整），与 T11 无关
