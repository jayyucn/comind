# T12 实现记录 — 2026-07-15

## T12 · date-ref 索引 — 完成 ✅

### 文件

| 文件 | 说明 |
|------|------|
| `src/storage/date-ref-index.ts` | DateRefIndex 类（核心索引 + 查询） |
| `src/composables/useDateRefIndex.ts` | 桥接 composable（Pinia block store → 索引） |
| `src/storage/date-ref-index.test.ts` | 22 项测试 |

### 数据结构

```
byBlock: Map<blockId, DateRef[]>
byKind:  Map<kind, Map<date, Set<blockId>>>
```

### API

| 方法 | 参数 | 返回 |
|------|------|------|
| `build(blocks)` | `{id, content}[]` | void |
| `update(blockId, content)` | string + string\|null | void |
| `remove(blockId)` | string | void |
| `queryByDateRange(kind, from, to)` | kind\|'*' + YYYY-MM-DD | IndexEntry[] |
| `queryOverdue(now?)` | YYYY-MM-DD | IndexEntry[] |
| `queryByDate(date, kind?)` | YYYY-MM-DD | IndexEntry[] |
| `getBlockRefs(blockId)` | string | DateRef[] \| undefined |

### 测试覆盖率

- build: 空列表、含 dateRef、多 dateRef
- update: 内容变更、删除、不存在的 block 安全
- remove: 完整移除
- queryByDateRange: 单日、跨日、所有 kind、无匹配、排序、带时间 ISO
- queryOverdue: 逾期 deadline、无逾期、schedule 不算逾期
- queryByDate: 全部 kind、按 kind 限定
- getBlockRefs: 存在/不存在
- 增量更新连续性: byKind 索引一致性、多 block 同一天增删
- 总计 **22/22**
