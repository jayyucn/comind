# T14 实现记录 — 2026-07-15

## T14 · 展示组件清理 — 完成 ✅

### 修改文件

| 文件 | 改动 |
|------|------|
| `src/components/Block/PropertyDisplay.vue` | ① `visibleProperties` 过滤掉 `deadline`/`scheduled` ② `getIcon` 移除 deadline/scheduled case ③ `getLabel` 移除 deadline/scheduled case |
| `src/components/Block/PropertyInline.vue` | ① `getIcon` 移除 deadline/scheduled case ② `getLabel` 移除 deadline/scheduled case（过滤器自动排除，因为内置定义已移除） |
| `src/components/Block/PropertyDisplay.test.ts` | 测试数据从 `deadline` 替换为 `area`（仍保留的内置属性） |

### 验收

- ✅ 日期属性不再以右侧徽章或属性面板出现（`deadline`/`scheduled` 在 `visibleProperties` 中被过滤）
- ✅ `status`/`priority` 徽章保留不变
- ✅ 全量测试 56 失败（比修复前 -1），无新增失败

### 与 T15 衔接

T15（存量数据迁移）是条件执行 — 若项目有存量数据需要迁移旧 property 格式 → 新的 inline dateRef 格式。如果确认无存量数据，可跳过。
