# T13 实现记录 — 2026-07-15

## T13 · property store 清理 — 完成 ✅

### 修改文件

| 文件 | 改动 |
|------|------|
| `src/types/property.ts` | ① 移除 `deadline`/`scheduled` 内置属性定义 ② `PropertyType` 去掉 `datetime` ③ `PropertyValueMap` 去掉 `datetime` 映射 |
| `src/utils/property.ts` | ① 移除 `datetime` case 分支 ② 移除 `datetime` 类型推断 ③ `date` case 简化为通用日期解析 |
| `src/stores/property.ts` | 注释清理（datetime → date） |

### 验收

- ✅ `deadline`/`scheduled` 不再在 `BUILT_IN_PROPERTIES` 中出现
- ✅ `PropertyType` 不再包含 `datetime`
- ✅ `setProperty`（通用函数）仍然可以处理任意属性键（如 `project`/`area`），但日期类属性不再有内置定义
- ✅ `status`/`priority` 内置属性行为不变
- ✅ 全量测试 **无新增失败**（57 失败全为历史遗留）

### 与 T14 衔接

T13 只是清理了**类型定义和内置属性注册**。T14 将清理**展示组件**（PropertyDisplay / PropertyInline）中 date 相关的渲染逻辑。
